[pytest]
testpaths = tests
markers =
    gpu: requires a CUDA-capable GPU. Auto-skipped by tests/conftest.py when torch is not importable or torch.cuda.is_available() is False. Never runs on this build machine.
addopts = -ra --strict-markers
