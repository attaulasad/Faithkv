# Full environment for the GPU host (installed via scripts/setup_vast.sh).
# This machine (build-only, no GPU) does NOT install torch/transformers with
# a torch backend; see docs/GPU_VALIDATION_PLAN.md. The CPU-only subset used
# during this build is pydantic/numpy/scipy/PyYAML (pyproject.toml core deps)
# plus transformers (tokenizer-only, no torch backend) for docs/PROBE_PROTOCOL.md.

# --- core (also in pyproject.toml [project.dependencies]) ---
pydantic>=2.6,<3
numpy>=1.26,<3
scipy>=1.11,<2
PyYAML>=6.0,<7

# --- GPU host only ---
# transformers upper bound is load-bearing, not cosmetic: the pinned R-KV
# monkeypatch (third_party/R-KV/HuggingFace, commit
# 45eaa7d69d20b7388321f077020a610d9afb65bd) replaces Qwen2Attention/
# Qwen2ForCausalLM internals shaped for the 4.48-4.55 attention interface
# (ALL_ATTENTION_FUNCTIONS, ...) and reaches into DynamicCache.key_cache/
# value_cache directly. transformers>=4.56 restructures both and "produces
# garbled output" per upstream's own requirements.txt comment. See
# docs/UPSTREAM_AUDIT.md H6 and §3.6.
transformers==4.55.4
torch==2.6.0
# Exact GPU-validation wheel. The previous open range selected a binary that
# imported with an undefined c10::Error symbol under Python 3.12 / Torch
# 2.6.0+cu124. This wheel was verified on the actual RTX 3090 validation host.
# Do not broaden this range during protocol-v3 validation.
flash-attn @ https://github.com/Dao-AILab/flash-attention/releases/download/v2.7.4.post1/flash_attn-2.7.4.post1+cu12torch2.6cxx11abiFALSE-cp312-cp312-linux_x86_64.whl#sha256=7e0b07913d56782d0f3f2ee76bd39587557742628983f6ab9ec2527c99437476

datasets>=2.19,<4
huggingface_hub>=0.24
jinja2>=3.1
matplotlib>=3.8

# kvcot.utils.math_verifier (B1A-2, discovery track only -- CLAUDE.md
# §1a/§4a). The eventual GPU discovery environment also grades MATH-500, so
# this belongs alongside the rest of this file's runtime deps, not only in
# pyproject.toml's cpu-tools extra. Exact pin, matching pyproject.toml.
math-verify[antlr4_13_2]==0.9.0

# --- dev/test ---
pytest>=8.0
pytest-cov>=5.0
accelerate==1.10.1
